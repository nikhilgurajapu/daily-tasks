"""
we can't use any indexing or slicing on sets
we cant access the elements in sets
we acn add or remove elemets in sets
set methods
by adding we use
update()add or update is done sequencially
add()

by removing we use
remove() removes the specific element and it returns exeption when the elements does not exist
discard()removes the specific elements in the set and doesnt return any exeption if element doesnt exists
pop() here pop() removes the 1st element
clear() it clear the entire set
del it deletes the set completely from python memory
"""

#set doesn't allows any duplicate value
#set prints true as a 1 and false as 0 if set existing integers
