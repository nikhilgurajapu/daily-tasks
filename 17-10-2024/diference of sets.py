set_a = {4, 2, 8}
set_b = {1, 2}
diff = set_a - set_b # "-" indicates the difence and returns uncomon elements from both the lists
print(diff)


"""set_a = {4, 2, 8}
tuple_a = (1, 2)
diff = set_a.difference(tuple_a)  #we can also use this method like this
print(diff)"""